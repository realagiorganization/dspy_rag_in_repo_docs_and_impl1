# Repository TODO

Use this file as the shared cleanup ledger for ongoing repository work.

## Active

- Confirm that the content-linting tool configs under `tools/` remain wired into CI after future workflow edits.
- Keep `docs/audit/2026-03-28-docs-site-and-test-plan.md` aligned with the real verification surface when tests or gates change.
- Add a browser-driven UI check once the static `render-ui` surface grows into a served interaction flow.
- Confirm that the first successful `GitHub Pages` deployment prominently exposes the generated test plan.
- Remove or close TODO items in the same turn that completes them.
